@model BookCave.Models.InputModels.EmployeeLoginInputModel

<section>
    <form asp-action="AddEmployee" asp-controller="Manage">
        <!-- Grid row -->
        <div class="form-group row">
            <!-- Default input -->
            <label for="inputUsername" class="col-sm-2 col-form-label-lg">Username</label>
            <div class="col-sm-10">
                <input type="text" asp-for="Email" class="form-control form-control-lg" id="inputUsername" placeholder="Username">
            </div>
        </div>
        <!-- Grid row -->

        <!-- Grid row -->
        <div class="form-group row">
            <!-- Default input -->
            <label for="inputPassword" class="col-sm-2 col-form-label-lg">Password</label>
            <div class="col-sm-10">
                <input type="text" asp-for="Password" class="form-control form-control-lg" id="inputPassword" placeholder="Password">
            </div>
        </div>
        <!-- Grid row -->

        <!-- Grid row -->
        <div class="form-group row">
            <div class="col-sm-3 offset-sm-10">
                <button type="submit" class="btn btn-primary btn-lg">Create employee</button>
            </div>
        </div>
        <!-- Grid row -->
    </form>
    <!-- Default horizontal form -->

</section>